import { describe, expect, test } from "vitest";

describe("Admin can create a new event", async () => {
  test("Call HTTP to create new event", () => {
    
    const responseEqual = 201

    

    expect(responseEqual).toEqual(201);
  });
});
